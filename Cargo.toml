[package]
name = "vimspell"
version = "0.1.0"
edition = "2024"

[profile.release]
debug = 1

[dependencies]
hashbrown = { version = "0.16.1", default-features = false, features = [
    "inline-more",
    "default-hasher",
] }
jsony = "0.1.8"
